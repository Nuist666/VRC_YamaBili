# 更新履历（Changelog）

> 本文件是**版本号与更新内容的唯一出处**。其他文档只写行为与做法，版本相关描述一律回指这里。
> 仓库：`https://github.com/Nuist666/VRC_YamaBili`，模块版本常量：`Editor/BilibiliSearchPanelSetup.cs` 的 `Version` / `Changelog`。

---

## 未发布

- 修复 Bake 使用过期 UdonSharp 字段缓存的问题：编译并重载脚本后自动继续，校验 Udon 写入成功后再保存。

- Direct Action URLs 增加按最新编号、每日增长及覆盖天数估算容量，含 20% 余量与分页记录；展示末编号和剩余天数。
- 新模块默认池扩为 120 万条，上限 200 万条；旧模块须手动应用估算并烘焙、上传。大池的 Unity/Udon 编译、包体及客户端内存仍需实测。
- 增加容量规划和整数边界回归断言。

## v1.1.0（2026-09-21）

主题：关键词搜索的**直接操作** —— 翻页、播放、入队不再需要玩家复制粘贴。

### 交互

- **翻页直接刷新**：`上一页` / `下一页` 点击后直接请求对应搜索页，不再弹出复制粘贴确认框。
- **播放直接执行**：结果行的播放按钮按该行 `recordsid` 查表取得完整地址并直接播放；当前已有视频在播时改为入队，并同时检查队列权限。
- **入队直接执行**：首次点击即可入队，不再需要先粘贴一次播放链接；每条结果保留 10 秒防重复冷却。
- **保留的确认框**：只有「复制链接」仍打开复制窗口，复制的是 B 站视频网页地址。

### 健壮性

- 新增三条状态提示：结果不可直接操作（`recordUnavailable`）、结果已过期需重搜（`staleResults`）、操作过于频繁（`cooldown`）。
- 结果行带 `recordsid`，面板在动作前校验「当前结果数组」与「该行显示的 BV / recordsid」是否一致，多面板场景下拒绝陈旧行，防止误播新搜索里的同一位置。
- 编号无效、编号池未生成或与 Base URL 不一致时只提示，不弹手动粘贴框，也不会取模误播。
- 坏 JSON / 下载失败保留上一页数据与分页编号；下一页返回空数组时保留返回上一页的原始请求地址。

### 编辑器工具

- 新增 **Direct Action URLs** 工具：按编号范围烘出完整 `?srid=` 地址池（默认 `550000`–`649999`，即起始 550000、数量 100000，上限 200000），并同时更新模块版本、版本按钮、版本浮层与日语更新履历，不重新生成 UI 布局。
- 新增 **Run Direct Action Regression Tests**：覆盖原始分页、结果截断、编号边界、坏 JSON 保留旧页、空页、错误地址、旧结果拒绝等断言。
- prefab 生成改为**等待 UdonSharp 程序资产升级完成**后再编译与序列化（首次安装时自动补齐缺失的程序资产，期间 Console 显示 `Preparing UdonSharp programs`），并补上失败清理与「已在生成中」的保护。
- 生成时会自动烘焙编号池；世界构建时检查活动模块的池是否有效，必要时按该模块配置重建（不自动改变编号范围）。

### 版本与文案

- 模块定义版本、顶栏版本按钮、版本浮层标题与更新履历统一为 `v1.1.0`。
- 六种语言的运行期本地化补齐上述三条新提示。
- 许可与署名条款**未变更**（见 [LICENSE.md](LICENSE.md) / [NOTICE.md](NOTICE.md)）。

### 范围限制（必读）

Udon 不能在运行时把字符串转成 `VRCUrl`，因此播放、入队与翻页都只能用编辑器预置的地址池。编号池有限、后端编号增长后需要重新烘焙并重传世界。详见 [DIRECT_ACTIONS.md](DIRECT_ACTIONS.md)。

### 本地验证（2026-09-21）

Unity 2022.3.22f1 / SDK Worlds 3.10.5：23 项回归断言通过，Udon Editor 与 World 编译通过，模块与独立面板的版本文字已验证。ClientSim 调用真实按钮事件完成真实后端搜索、下一页与上一页请求；播放按钮把对应 srid 直接交给 AVPro；首次点击入队后队列由 0 条变为 1 条，记录 URL 与所选视频一致，过程中确认框保持关闭。

> ClientSim 的媒体模拟不等于 VRChat 中的视频解码成功，多人同步仍需真实客户端验收。

---

## v1.0.0（2026-09-21）

- 首次发布：YamaPlayer 屏幕 UI 的 B 站面板。
- **网址输入**标签页（默认）：`{Base URL}?url=` 前缀 + 玩家补全 BV 号或完整 B 站链接，输入即播；在播时改为入队。
- **关键词搜索**标签页：`{Base URL}?page=1&keyword=` + 结果列表（标题 / UP主 + BV号 / 简介），支持复制链接、播放、加入待播队列（10 秒防重复）。
- 翻页、播放、入队当时需要玩家粘贴一次播放地址并确认（VRChat 不允许脚本构造 `VRCUrl`）；v1.1.0 起移除。
- 版本浮层：作者头像、社交账号、AI 署名、更新履历；9 种语言；配色跟随 YamaPlayer `ColorDefinition`。
- 自带结果列表 `BilibiliResultList.cs`，不依赖对 YamaPlayer 核心的修改。
